/**
 * Font family tokens for the Lingua design system.
 * These names match the keys the Poppins .ttf files are registered
 * under via `useFonts` in `app/_layout.tsx`, and the `--font-poppins-*`
 * CSS variables in `global.css`.
 */

export const Fonts = {
  regular: "Poppins-Regular",
  medium: "Poppins-Medium",
  semiBold: "Poppins-SemiBold",
  bold: "Poppins-Bold",
} as const;
